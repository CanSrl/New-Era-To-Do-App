# Synthesis Summary

Entry point for downstream consumers (`gsd-roadmapper`). Generated by
`gsd-doc-synthesizer` from `.planning/intel/classifications/`.

Mode: `new`
Precedence applied: `ADR > SPEC > PRD > DOC` (no per-doc override present)

---

## Doc counts by type

| Type | Count |
| --- | --- |
| ADR | 0 |
| SPEC | 2 |
| PRD | 0 |
| DOC | 0 |
| UNKNOWN | 0 |

Both ingested documents describe the same feature slice: **niş modül 1. dilim —
müşteriler ve projeler** (CLAUDE.md Faz 5, birinci dilim).

Sources:
- `docs/superpowers/specs/2026-08-14-nis-modul-musteri-proje-design.md`
  (SPEC, high confidence, `locked: false`) — teknik tasarım
- `docs/superpowers/plans/2026-08-14-nis-modul-musteri-proje.md`
  (SPEC, medium confidence, `locked: false`) — uygulama planı, 10 görev

## Cross-ref graph

2 nodes, max depth 2, **no cycles**.
`plans/...-nis-modul-musteri-proje.md` → `specs/...-design.md` → `CLAUDE.md`,
`supabase/migrations/20260814120000_niche_module.sql` (ingest kümesi dışı,
terminal düğümler).

## Decisions locked

0. Hiçbir doküman `locked: true` taşımıyor, hiçbiri ADR olarak
sınıflandırılmadı. `decisions.md` yazılmadı.

## Requirements extracted

0. Hiçbir doküman PRD olarak sınıflandırılmadı. `requirements.md` yazılmadı —
tip ataması uydurulmadı.

## Constraints

**36 entry** → `.planning/intel/constraints.md`

| type | count |
| --- | --- |
| schema | 12 |
| protocol | 9 |
| api-contract | 9 |
| nfr | 10 |

Kapsam başlıkları: `clients`/`projects` şeması ve bileşik yabancı anahtarlar,
müşteri silme tetikleyicisi, GRANT+RLS, istemci tipleri, store alanları ve
sürüm 5 göçü, senkron sırası, zincirleme id yeniden eşleme, `pendingCount`,
repository yüzeyi, `/app/clients`, TaskForm seçicileri, `/app/delivery`,
`nicheModule` bayrağı, i18n, erişilebilirlik, test kapıları, kabul edilen borç,
kapsam sınırı ve riskler.

Bunlardan **2 tanesi (CON-35a, CON-35b) rakip varyanttır** ve birleştirilmemiş,
ayrı id'lerle korunmuştur.

## Context topics

0. Hiçbir doküman DOC olarak sınıflandırılmadı. `context.md` yazılmadı.

## Conflicts

- **0 blockers**
- **1 competing variant** — birleştirme fonksiyonu yapısı
  (`mergeCategories` kopyası vs. jenerik `mergeNamed` çekirdeği). İki SPEC eşit
  precedence taşıdığı için precedence kuralları kazananı seçemez; SPEC-PLAN bu
  sapmayı zaten "onayınıza sunulan" olarak işaretliyor.
- **3 auto-resolved / informational** — `set search_path` sıkılaştırması,
  `remapTaskLinks` imza somutlaştırması, SPEC-PLAN'ın medium confidence
  sınıflandırması.

Detay: `.planning/INGEST-CONFLICTS.md`

## Files

- `.planning/intel/constraints.md` — sentezlenmiş kısıtlar (tek intel dosyası)
- `.planning/INGEST-CONFLICTS.md` — çakışma raporu
- `.planning/intel/classifications/` — kaynak sınıflandırmalar

## Gate status

**AWAITING USER** — 1 competing variant yönlendirme öncesi çözülmeli.
Blocker yok, dolayısıyla intel dosyaları yazıldı; ancak
`doc-conflict-engine.md` gereği WARNING açık onay almadan geçilemez.
